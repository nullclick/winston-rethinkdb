
describe '#stream', ->

	describe 'before transport is ready', ->

		it 'should still return a stream object'
		it 'should start emitting once able to'
		it 'should support attaching to pre-created stream'
		it 'should close cleanly'


	describe 'after transport is ready', ->

		it 'should still return a stream object'
		it 'should start emitting once able to'
		it 'should support attaching to pre-created stream'
		it 'should close cleanly'


	describe 'with multiple streams', ->

		it 'should still return a stream object'
		it 'should start emitting once able to'
		it 'should support attaching to pre-created stream'
		it 'should close cleanly'
	